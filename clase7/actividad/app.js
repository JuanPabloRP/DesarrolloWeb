const algoXD = document.getElementById('algunaChimbadaXD');

const infoSinSentido = ['anasheee', 'douu', 'XDd'];

infoSinSentido.forEach((i) => {
	const elem = (i.createElement = document.createElement('p'));
	elem.className = 'textoXD';
	elem.innerHTML = i;
	algoXD.append(elem);
});
